namespace Common.Utils
{
    public static class MenuItemPaths
    {
        private const string Slash = "/";
        
        public const string Tools = nameof(Tools) + Slash;
        public const string AssetsTools = "Assets" + Slash + Tools;
    }
}